1. Open the folder you clone the repo in,
2. Run "cd Ecommerce" (Terminal command) - at present directory,  
(you should be able to see backend, frontend, ...),
3. Run "npm run dev" ,
4. cd frontend (In a new/split Terminal),
5. Run "npm i" followed by "npm start" to open the localhost:3000 server in default browser.
