1. Open the folder you clone the repo in (you should be able to see backend, frontend, ...),
2. Run "npm run dev" (Terminal command) - at present directory,
3. cd frontend (In a new/split Terminal),
4. Run "npm start" to open the localhost:3000 server in default browser.
